apiVersion: v1
kind: Service
metadata:
  name: kaui
spec:
  type: ClusterIP

  ports:
    - port: {{ .Values.kaui.service.port }}
      targetPort: 9090
      protocol: TCP
      name: http

  selector:
    app: kaui
